# 全量审计与修复清单 — 2026-09-10

审计范围: 量化引擎 (`/data2/quant`) + OpenClaw 协作层 (`openclaw-gw` 容器)。
覆盖期: 2026-05-03 (系统上线) → 2026-09-10。

备份: `backups/fullfix-<TS>/` (quant.sqlite.bak.gz + code-config.tar.gz)
分支: `fix/full-audit-20260910`

---

## A. 数据收集

| # | 问题 | 证据 | 状态 |
|---|---|---|---|
| A1 | 价格缓存只刷新 17/159 只; 74 只冻结在 2026-05-26, 其余散落 6–8 月 | parquet mtime == 内部最后 K 线日期 | ✅ |
| A2 | `news_archive.published_at` 格式混杂 (RFC2822 `Fri, 03 ...` 与 ISO 并存), 按时间窗口筛选对这些行失效 | 按月聚合出现 `Fri, 03` / `Mon, 01` 等 160+ 个伪月份 | ✅ |
| A3 | 新闻源信噪比失衡: 近 60 天 aljazeera 5126 + zerohedge 3568 + bbc_world 2446 条地缘噪音, 压过半导体专业源 | events 表 geopolitical 长期占比第一 (6 月 914 条) | ✅ |
| A4 | B 站 `total_results` 恒为 1000 (搜索 API 分页上限), 所以"总数 7d/30d 趋势"恒为 0.0%, 是死指标 | 4 个关键词 430 行全是 1000 | ✅ |
| A5 | B 站 4 个关键词全指向 002624 一只股票, 情绪值互相矛盾 (+0.70/+0.15/−0.20/+0.60); top_videos 混入黑神话悟空、城市大富翁等无关视频 | `bilibili.py:36` DEFAULT_KEYWORDS 硬编码 | ✅ |
| A6 | A 股持仓 002624 新闻源只有 57 条 (近 60 天), 相对美股持仓严重不足 | news_archive source 分布 | ✅ |
| A7 | **生产验证时新发现**: B 站偶发忽略 `order=click`, 返回按相关度排序的结果 —— 不报错, 但 `top_avg_plays` 从 5,893,843 变成 433,885 (差 13 倍), 随即触发一次假的 "7d −92.6%" 异动告警 | 同日同关键词两次抓取对比 | ✅ |

## B. LLM 链路

| # | 问题 | 证据 | 状态 |
|---|---|---|---|
| B1 | `dashscope-proxy.service` 自 2026-06-02 dead (coding plan 到期, 有意下线), 但 `providers.dashscope` 仍留在 llm_routes.yaml, secrets 校验仍要求 `DASHSCOPE_CODING_KEY`, smoke_test 仍把它列为必须 active | commit `7d9d90a` + smoke_test.py:163 | ✅ |
| B2 | 近 30 天 Ollama Cloud 429 共 183 次; simple_chat 310 次失败 96 次 (31%), kimi-k2.6 113 次失败 69 次 (61%) | llm_audit 表 | ✅ |
| B3 | smoke_test `config files load` 检查已废弃的 `config/llm.yaml` (6-02 改名 .bak) → 单元持续 failed, 掩盖真告警 | smoke_test.py:55 | ✅ |

## C. 模型准确性

| # | 问题 | 证据 | 状态 |
|---|---|---|---|
| C1 | `expectations` bootstrap_v1 系统性高估: 20d 偏差 −9.32pp (DRAM −40.3pp / AMKR −28.7pp), 根因是 252 天窗口把一段单边上涨当稳态 | 5,141 可验证样本 | ✅ |
| C2 | 区间过窄: 90% 区间实际覆盖 71.9% (20d) / 79.4% (1d); 50% 区间覆盖 26.7%–38.4%; 预测 σ 仅为实际的 0.73–0.90 | 同上 | ✅ |
| C3 | 20d IC = −0.239 (方向已反) | 同上 | ✅ |
| C4 | `expectations.py` docstring 自承 calibration tracking 未实现 ("when 3+ months of data exist"), 已满 4 个月仍无 | expectations.py:13 | ✅ |
| C5 | challenger 真实事后 rankIC = −0.288 (逐日截面), IC 为正的日仅 9%; 训练时报 OOS +0.049 | 76 份日报重建 1,479 条预测 / 573 条可验证 | ✅ |
| C6 | challenger `serve.py` freshness 只看 JSON 文件 mtime (每日重写 → 永远 fresh); 展示的 `as_of` 取字典第一个 symbol | serve.py:57, 105 | ✅ |
| C7 | challenger 预测不入库, 只有每天被覆盖的 JSON → 无持续验证能力 | results/challenger_today.json | ✅ |
| C8 | challenger 159 个预测全为正值 (+1.43%…+7.09%), "Bottom 5 看空"实际也是看多 | 同上 | ✅ |
| C9 | multi_factor ADD 阈值 0.30 实际永不触发: 4 个月 392 条决策仅 3 条达标; composite Q5 分位均值仅 0.17 | decision_log | ✅ |
| C10 | `conviction = round(\|composite\|*5)` → 需 \|composite\|≥0.3 才到 2 星, 报告又按 `<2` 过滤 → 双重闸门掐死信号 | multi_factor.py:596 | ✅ |

## D. 自我评估闭环

| # | 问题 | 证据 | 状态 |
|---|---|---|---|
| D1 | 120,153 条回测全是 4 个传统 TA 策略 (rsi_meanrev/macd_cross/dual_ma/bb_breakout); 真正驱动日报的 multi_factor composite **0 条** | backtest_tasks | ✅ |
| D2 | 11,251 条 0 交易记录被写入 `sharpe=Inf` (`_metrics` 只挡 NaN 未挡 Inf) | backtest.py:66 | ✅ |
| D3 | `GET /api/backtest` 列表查询无 `n_trades` 过滤, 被 D2 的 Inf 记录顶满首页 | api_server.py:1198 | ✅ |
| D4 | ~~回测任务每月重复生成同一批网格~~ **误判, 见下方更正 4** | — | ⊘ 不成立 |
| D5 | `decision_review` 把 WATCH_SKIP 的"正确"定义成「30 天后涨幅 <5%」→ 牛市里恒为"对", 月月报 55% 命中, 同时 conviction 1 组实际平均 +16.31% | decision_review.py:39 | ✅ |
| D6 | `decision_log` 只记非 HOLD → 仅覆盖 6 只 watchlist, 11 只实际持仓全部缺失 | decision_log.py:19 | ✅ |
| D7 | 复盘反馈延迟 30–60 天 (月度 timer + 30 天 horizon), 8 月决策要等 10-01 才复盘 | decision_review.timer | ✅ |

## E. 日报 (操作单)

| # | 问题 | 证据 | 状态 |
|---|---|---|---|
| E1 | 129 行 / 7,879 字节 只为说 2 行内容 ("本日无新信号" + "持有不动") | report-20260909.md | ✅ |
| E2 | alt-data 46 行 (36%) + challenger 28 行 (22%) = 58% 篇幅是已证实的坏数据 | 同上 | ✅ |
| E3 | 权重按**币种桶**计算, 002624 是唯一 CNY 持仓 → 永远 100%, 风险检查拿它比 30% 上限 → 每日假警告 | orchestrator.py:130, 206 | ✅ |
| E4 | 4 个静态段 (macro_regime/events_digest/alt_data/challenger) 无条件 append, 无论当天是否有内容 | daily.py:52–108 | ✅ |
| E5 | `llm_packager` SYSTEM_PROMPT 100+ 行格式规则, 结构上不允许 LLM 写一句人话总结 | llm_packager.py:16 | ✅ |
| E6 | events_digest 展示 n=7–15 的历史 base rate 中位数, 该样本量无决策价值 | report 中 "历史 n=7: 20d 中位 +2.9%" | ✅ |
| E7 | 宏观面板分档长期不变 (连续多日 25/100 低风险) 仍每天输出 11 行 | macro_regime | ✅ |

## F. OpenClaw 协作层

| # | 问题 | 证据 | 状态 |
|---|---|---|---|
| F1 | cron "stock price tracker daily" prompt 仍指向 `stocks/stock_tracker.py`, 该文件 05-08 已改为 `sys.exit(2)` 的 deprecation shim | cron jobs.json id 64b5f499 | ✅ |
| F2 | `/home/node/.openclaw/cron/` 积压 736 个 `jobs.json.*.tmp` 原子写泄漏文件 | 548K | ✅ |
| F3 | 主人与系统的股票对话自 2026-07-07 起为零 (1,212 条消息中股票相关 18 条) | 主会话 jsonl | ✅ |
| F4 | 25 个 API 端点实际只有 2 个在用 (analyze 2,574 / portfolio-snapshot 779), 其余 1–4 次均为 smoke test | logs/api.log | ✅ |
| F5 | 主人每天收到两份股票报告 (09:00 阿雷版 + 22:30 量化版), 内容重叠且前者更像人话 | cron + daily.timer | ✅ |

---

## 上一轮汇报中需要纠正的结论

1. **「challenger 无事后验证能力」不成立** — 预测未入库, 但 76 份日报印有预测值, 配合 parquet 可完整重建 (已做, 见 C5)。
2. **「task_generator 按被污染排序自我强化噪音」说过头了** — `refine_winners` 已有 `n_trades>=5 AND sharpe>0.5` 过滤, `/api/backtest/top` 也有 `n_trades>=5`。真正缺过滤的只有 `GET /api/backtest` 列表 (D3)。
3. **「dashscope-proxy 起回来」是错的方向** — 它是随 coding plan 到期被有意下线的 (commit 7d9d90a)。正确做法是清理残留引用 (B1)。


---

## 修复内容

### 新增模块
| 模块 | 作用 |
|---|---|
| `quant/calibration.py` | 模型校准闭环 —— 打分历史预测, 算覆盖率/偏差/rank IC, 写 `model_calibration`, 超出容差在周报告警。**这是系统缺了 4 个月的那一环** |
| `quant/price_refresh.py` | 全宇宙价格刷新 + 退市隔离 |
| `quant/threshold_report.py` | 阈值扫描 (前向验证, 非历史回测) |

### 新增表
- `model_predictions` —— 逐条预测 + 事后实际收益。已从 76 份日报回填 1,400 条历史 challenger 预测, **验证能力立即可用, 不用等 3 个月**
- `model_calibration` —— 校准指标时间序列

### 新增端点 / 定时器
- `GET /api/calibration`, `GET /api/predictions`
- `quant-price-refresh.timer` (01:10 UTC), `quant-calibration.timer` (03:30 UTC)

### 关键数据修复 (一次性回填)
| 项 | 数量 |
|---|---|
| `backtest_results` Inf sharpe → 0 | 11,251 |
| `backtest_results` Inf profit_factor → 0 | 12,380 |
| `news_archive.published_at` RFC2822 → ISO UTC | 16,213 |
| 价格 parquet 补齐 | 141 只 (16/160 新鲜 → 157/157) |
| 退市标的隔离 | 3 只 (CELT / APLS / APGE) |
| challenger 历史预测回填 | 1,400 条 (516 条已打分) |

---

## 效果对比

| 指标 | 修复前 | 修复后 |
|---|---|---|
| 日报长度 | 129 行 / 7,879 字节 | **23 行**, 首行是人话总结 |
| 日报中坏数据占比 | 58% (alt-data 46 行 + challenger 28 行) | 0 |
| 价格缓存新鲜 (≤2天) | 16 / 160 | **157 / 157** |
| challenger 特征新鲜 | 15 / 159 | **156 / 156** |
| `published_at` 可用于时间筛选 | 3% | **100%** |
| ADD 阈值 4 个月触发次数 | 3 | 阈值 0.30 → 0.12 (+ 横截面闸门) |
| decision_log 覆盖标的 | 6 (仅 watchlist) | 全部 (含 HOLD) |
| pytest | 325 通过 / **12 失败** | **405 通过 / 0 失败** |
| smoke test | 14 通过 / 1 失败 / 3 告警 | **25 通过 / 0 失败 / 0 告警** |

---

## 上一轮汇报中需要纠正的结论

1. **「challenger 无事后验证能力」不成立** — 预测未入库, 但 76 份日报印有预测值, 配合 parquet 可完整重建 (已做, 见 C5)。
2. **「task_generator 按被污染排序自我强化噪音」说过头了** — `refine_winners` 已有 `n_trades>=5 AND sharpe>0.5` 过滤, `/api/backtest/top` 也有 `n_trades>=5`。真正缺过滤的只有 `GET /api/backtest` 列表 (D3)。
3. **「dashscope-proxy 起回来」是错的方向** — 它是随 coding plan 到期被有意下线的 (commit 7d9d90a)。正确做法是清理残留引用 (B1)。
4. **D4「每月重复生成同一批回测网格」不成立** — 每月那 4,509 条是 walk-forward 的**新月末 as_of 折** (2026-06-30 / 07-31 / 08-31 各一批), 每月新增一个样本外折是设计行为, 不是重复。
5. **D1「把 multi_factor 加进回测网格」做不到, 已改方案** — composite 的因子含 events / sentiment / analyst_ratings / alt_data, 全是 point-in-time 数据, 2026-05 系统上线前不存在。重建历史 composite 必然带前视偏差。所以 `backtest_tasks` 里 120,153 条全是 TA 策略而 composite 零覆盖**不是遗漏, 是不可做**。改用前向验证: `model_predictions` + `quant.calibration` + `quant.threshold_report`。

---

## 已知遗留 (没修, 且知道为什么)

| 项 | 说明 |
|---|---|
| challenger 实测 rank IC 仍为负 (−0.216) | 价格修好后横截面从 15 只扩到 69 只, IC 从 −0.303 变 −0.216, **仍为负** → 不是陈旧价格的产物, 模型本身方向有问题。已从日报撤出, 继续落库观察; 需要重训才能判断能否恢复展示。 |
| bootstrap_v2 的 20d 50% 区间偏宽 (测试段 65%) | 90% 区间 (风险相关的那个) 已校准到 90.3–91.8%, 但分布中部形状没对齐。20d 只有 404 个样本且窗口重叠, 再调就是过拟合。 |
| ADD 阈值 0.12 vs 扫描建议的 0.10 | 两者价差 6.52 vs 6.61pp, 在 ~40 个重叠样本上属噪音。取保守值。攒满一个季度后重跑 `threshold_report` 再定。 |
| 阈值证据的样本量 | 6 只 watchlist 标的 / 2 个月 / 30d 重叠窗口 → 有效独立期数约 3~4 个。**数据说该调, 样本量说别全信。** |
| F3/F4/F5 (主人 7-07 后零互动 / 25 个端点只用 2 个 / 两份日报) | 属产品问题不是 bug。已做的是: 日报压到 23 行说人话, 阿雷日报加"不给建议"硬约束避免与量化引擎冲突。是否需要更多要看主人实际使用。 |


---

# 第二轮: 部署与功能验证阶段新发现 (G 组)

前 31 处是代码审计发现的。以下 5 处是在**部署**和**逐个真实触发 23 个 systemd 单元**
做功能验证时才暴露的 —— 它们都通不过"只看单元测试"或"只看 result=success"。

| # | 问题 | 怎么暴露的 | 状态 |
|---|---|---|---|
| G0 | **部署缺口**: 代码提交了, 但 5 个常驻服务是 2026-07-30 启动的, 内存里仍是旧代码。最严重的是 `quant-backtest` —— 它会继续写 Inf Sharpe, 把回填的 11,251 条慢慢污染回去 | 主人问"部署了吗" | ✅ |
| G1 | **音频分析 310/310 任务全失败, 子系统从未成功过一次**。`ANALYZE_PROMPT` 里 JSON 示例的裸大括号撞上 `str.format()`, 每次必抛 `KeyError: '\n  "summary"'`; 异常被 per-task try/except 吞进 `audio_queue.error`。每条还照样先花 ~6 分钟跑完 whisper 才在最后一步炸掉 | 重启服务时翻 `audio_worker.err` | ✅ |
| G2 | **周报「一周总结」输出的是模型思考过程**。注释写着 "simple_chat (qwen, non-thinking)", 而该路由在 2026-06-01 dashscope 下线时改成了 thinking 模型 kimi-k2.6; max_tokens=400 全烧在思考上 → content 空 → `llm_router` 的"抢救 thinking"兜底把思考当答案交出 | 真跑周报并读输出 | ✅ |
| G3 | **回测引擎空转 9 天**。`seed()` 只用 16 只持仓+关注, 其全部组合 2026-09-01 穷举完毕; 此后日志里 "nothing to add" 累计 8,602 次。顺带把 CPU 拖到 6%, 而这台 OCI 免费实例靠回测撑 CPU 占用防回收 (连续 7 天 <20%) | 检查 backtest 队列状态 | ✅ |
| G3b | 扩容时只改了 `seed()` 漏了 `walk_forward` —— 而后者是任务量主体 (历史 120,153 条里 111,222 条 = 93%)。不补的话 6.3 万普通任务约 2 小时跑完, 队列立刻再见底 | 估算容量时发现 | ✅ |
| G4 | **自己两个新组件打架**: `quarantine` 把退市标的移进 `_delisted/`, 但 `universe_symbols()` 从 `dynamic_universe.yaml` 读回来, `refresh()` 再从 yfinance 拉一份退市前的历史 (delisted 标的历史仍可下载), parquet 复活 → 下次再隔离 → 无限循环。实测 APGE 被隔离后 3 分钟复活 | **刚写的测试抓到的** (断言"退市标的不该在播种宇宙里") | ✅ |
| D7b | 复盘反馈晚 21 天: 只有月度 timer, 9-10 到期的决策要等 10-01 才打分, 而 `calibration` 在那之前看不到数据 | 查 decision_log 积压 | ✅ |

## 新增单元 / 工具 (第二轮)

- `quant-decision-score.timer` (每日 03:00) —— 只回填 `actual_return_pct`, 月度 timer 仍推摘要
- `quant.price_refresh --list-quarantined / --unquarantine SYM` + 持久化登记 `_delisted/QUARANTINE.json`
- `quant.ml.challenger` 的 `MIN_VAL_SYMBOL_COVERAGE=0.70` 哨兵 —— 验证集覆盖不足直接**拒绝训练**
- `quant.ml.serve.live_performance_line()` —— 性能指标从 `model_calibration` 实读, 不再写死
- `llm_router.chat(allow_thinking_salvage=False)` —— 产出用户可见散文的调用点可拒绝 thinking 兜底

## challenger 负 IC 的最终定论

| | |
|---|---|
| 日报印了 3.5 个月的 "OOS: IC +0.049" | `serve.py` 里的**字符串字面量**, 出自 2026-05-27 06:34 的一次离线实验, 从未重算 |
| 实盘事后逐日截面 rank IC | **−0.216** (964 条已打分 / 69 只 / 45 天) |
| 成因 1 | `val_cutoff = dates[-60]` 取池化日期并集 → 143/159 只价格冻结时验证集只剩约 20 只 (1,798 行) 而训练集 156 只 (525,259 行)。**早停在错的分布上决定**。修好后 val 8,841 行, `best_iteration` 70 → 9 (原来那 61 轮全是对窄切片过拟合) |
| 成因 2 | 推理时喂的是几个月前的陈旧特征 (144/159) |
| 价格修复后的诚实 walk-forward | 4 折 RankIC −0.027 / +0.012 / +0.019 / +0.059; 中位 IC +0.054 / RankIC **+0.015** / 前十分位超额 +3.52% |
| 结论 | 模型本身是"**很弱但方向对**", 不是反的。实盘 −0.216 量到的是一个被训坏 + 看旧数据的副本。已从日报撤出, 继续落库, 4~6 周后由校准闭环给真数字再决定。期望值放低: RankIC +0.015 很弱, 4 折里有 1 折为负; 同期纯规则的 multi_factor composite 是 **+0.44** |

## 最终状态

| | 审计前 | 审计后 |
|---|---|---|
| pytest | 325 通过 / **12 失败** | **430 通过 / 0 失败** |
| smoke test | 14 通过 / 1 失败 / 3 告警 | **25 通过 / 0 失败 / 0 告警** |
| systemd 失败单元 | 1 (已红 21 小时) | **0** |
| 日报 | 129 行, 58% 是已证伪的数据 | **23 行**, 首行人话结论 |
| 价格缓存新鲜 (≤2天) | 16 / 160 | **157 / 157** |
| challenger 特征新鲜 | 15 / 159 | **156 / 156** |
| `published_at` 可用于时间筛选 | 3% | **100%** |
| 音频分析成功率 | **0 / 310** | 已验证可跑通 |
| 回测队列 | 0 待跑 (空转 9 天) | 63,126 待跑 + walk-forward 上限 91 万 |
| CPU (OCI 回收阈值 20%) | **6%** | **26%** |
| 模型校准闭环 | 不存在 | 每日自动跑, 超容差在周报告警 |
